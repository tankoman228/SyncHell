# --- Папки сборки/выходные данные ---
build/

# музыку для уровней качайте сами, а то мне АП прилетит
levels/*.ogg
levels/*.wav
levels/*.flac

# они слишком жирные, не влазят в репозиторий
winlibs/*

.cache

SyncHell-x86_64.AppImage
SyncHell.AppDir
